#Git Configure: We need to configure the git with your github account--------
----------------------------------------------------------------------------
git config --global user.name "Santanu.Sarkar88"
git config --global user.email "santanu8862@gmail.com
git config --list







#Commands-- pushing for first time
------------------------------------
git init
git add . 
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/SantanuSarkar88/Git_commands.git
git push -u origin main

#if you create any repo with read.me file there we need to run
git pull origin main --allow-unrelated-histories

#Commands for pushing code/file from 2nd time
git add .
git commit -m "commit msg"
git push

#Extra commands:
git branch: it will use to find list of branches
git branch <branchname>: use to create new branch
git checkout <branchname>: use to switch b/w branches
git branch -d <branchname>: To delete the branch
git pull : to get all recent updates remote branch
git merge <branchname> : to merge in local 
git log
git log --summary
git log --oneline --summary
git log --summary --stat : summary stats